//setTime関数
function sayHello() {
    console.log("Hello, World!");
  }
  
  setTimeout(sayHello, 3000); // 3秒後にsayHello()を実行
  //setTimeout関数は、最初の引数として渡された関数を、2番目の引数として渡されたミリ秒後に実行します。この場合、sayHello関数は3秒後に実行されます
//画像を自動的に切り替えるといった機能で活用可能